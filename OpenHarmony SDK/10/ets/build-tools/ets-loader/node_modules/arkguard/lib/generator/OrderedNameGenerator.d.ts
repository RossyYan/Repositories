import type { INameGenerator, NameGeneratorOptions } from './INameGenerator';
/**
 * @desc simple ordered name generator, e.g.: a, b, c, d, ..., a1, b1, c1, ...
 */
export declare class OrderedNameGenerator implements INameGenerator {
    private mCharIndex;
    private mLoopNumber;
    private mReservedNames;
    private readonly CHAR_COUNT;
    private readonly CHAR_CODE_A;
    constructor(options?: NameGeneratorOptions);
    private updateElements;
    getName(): string;
    reset(): void;
}
