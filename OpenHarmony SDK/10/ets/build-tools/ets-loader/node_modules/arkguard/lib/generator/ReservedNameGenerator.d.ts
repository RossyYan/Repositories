import type { INameGenerator, NameGeneratorOptions } from './INameGenerator';
/**
 * @Desc: a name generator which use reserved keywords similar words to get obfuscated name
 */
export declare class ReservedNameGenerator implements INameGenerator {
    private readonly mReservedNames;
    private readonly mTransformSet;
    private mIdIndex;
    private readonly mWordInfo;
    private mWordIndex;
    constructor(options?: NameGeneratorOptions);
    private getWordInfo;
    private transformName;
    /**
     * @return: null for end
     */
    getName(): string;
    reset(): void;
}
