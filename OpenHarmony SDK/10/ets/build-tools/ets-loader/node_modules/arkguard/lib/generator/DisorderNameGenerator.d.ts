import type { INameGenerator, NameGeneratorOptions } from './INameGenerator';
/**
 * @Desc: simple disordered name generator
 * e.g.: c, b, z, a, ..., d1, a1, z1, ...
 */
export declare class DisorderNameGenerator implements INameGenerator {
    private mCharIndex;
    private mLoopNumber;
    private mReservedNames;
    private readonly CHAR_COUNT;
    private readonly CHAR_CODE_A;
    private readonly mCharIndexList;
    constructor(options?: NameGeneratorOptions);
    private updateElements;
    getName(): string;
    reset(): void;
}
