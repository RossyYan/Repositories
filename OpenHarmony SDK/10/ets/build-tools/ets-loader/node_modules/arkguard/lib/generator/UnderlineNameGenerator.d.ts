import type { INameGenerator, NameGeneratorOptions } from './INameGenerator';
/**
 * @Desc: a name generator which use underline to get obfuscated name
 */
export declare class UnderlineNameGenerator implements INameGenerator {
    private readonly mMaxLength;
    private readonly mReservedNames;
    private mCurrentLength;
    constructor(options: NameGeneratorOptions);
    /**
     * @return: null for end
     */
    getName(): string;
    reset(): void;
}
