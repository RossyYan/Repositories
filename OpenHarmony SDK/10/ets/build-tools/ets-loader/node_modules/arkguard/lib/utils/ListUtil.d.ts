export declare class ListUtil {
    static readonly MAX_INIT_LEN: number;
    /**
     * get a list with element number filled for each element
     * @param length: list length you want init.
     */
    static getInitList(length: number): number[];
    /**
     * shuffle list
     * @param originList: list to be shuffled
     */
    static shuffle(originList: number[]): void;
    /**
     * merge two list to one list of unique element
     * @param listA
     * @param listB
     * @param listC
     */
    static uniqueMergeList(listA: string[], listB: string[], listC?: string[]): string[];
}
