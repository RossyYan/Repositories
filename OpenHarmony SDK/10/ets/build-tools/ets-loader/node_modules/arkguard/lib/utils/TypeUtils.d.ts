import type { SourceFile, TypeChecker } from 'typescript';
export declare class TypeUtils {
    /**
     * performing symbol analysis on the original abstract syntax tree can cause sourcemap errors
     * @param oldAst
     *
     */
    static createNewSourceFile(oldAst: SourceFile): SourceFile;
    static createChecker(ast: SourceFile): TypeChecker;
}
